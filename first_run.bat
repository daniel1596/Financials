echo This is not needed yet, but perhaps one day in the future it could be run on installation.
echo.
echo Tasks for this file would include:
echo - Calling a (Python?) script to clear all transaction data from the db (just in case), etc.
echo - Installing all Python packages via a requirements.txt file (to be done later)
echo - Maybe some sort of readme popping up on the screen? Not sure if that would be needed.
echo (Note - don't need to install npm. Vue is included via script reference.)
echo (SQLite doesn't need to be installed either. Python has it in the base library.)